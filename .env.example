# Copy this file to .env and fill in your values.
# The .env file is loaded automatically at startup by spring-dotenv.

# Bearer-token signing key — use a long, random string in production.
TOKEN_SIGNING_KEY=change-me-in-production

CONNECT_HOSTNAME=my-access-server
CONNECT_PORT=11001
